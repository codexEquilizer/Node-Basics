extends layouts/main-layout.pug

block content 
    main
        h1 404
        h2 #{message || "Sorry — the page you are looking for does not exist."}
        p You can return to the homepage or check the URL and try again.